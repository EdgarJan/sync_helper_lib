import Testing
@testable import SyncHelper

@Test func placeholder() async throws {
    // Integration tests require a running sync server and Firebase auth.
    // This placeholder confirms the package compiles.
}
